<!DOCTYPE html>
<html>
  <head>
    <meta charset="utf-8">
    <title>Trips</title>
  </head>
  <body>

      <h1>Welcome <%= user.user_name %> </h1>

      <h3>Upcoming Trips:</h3>
      <ul>
        <% for (let i = 0; i < trips.length; i++) { %>
          <b><%= trips[i].title %> From <%= trips[i].start %> to <%= trips[i].destination %></b><br>
        <% } %>
      </ul>

      <hr>

      <h3>Plan a trip:</h3>
      <form action="/trips" method="post">
        <input type="text" name="title" placeholder="Trip Title">
        <input type="text" name="trip_description" placeholder="Trip Desc">
        <select name="flight_id">
          <% for (let i = 0; i < flights.length; i++) { %>
            <option value="<%=flights[i].id%>"><%= flights[i].start %> - <%= flights[i].destination %></option>
          <% } %>
        </select>
        <input type="submit">
      </form>

  </body>
</html>
